---
apiVersion: v1
kind: Service
metadata:
  name: sentry-demo-node
  namespace: demo-node
  labels:
    app: sentry-demo-node
spec:
  type: ClusterIP
  selector:
    app: sentry-demo-node
  ports:
    - name: http
      port: 8080
      targetPort: http
